{
  "compilerOptions": {
    "jsx": "react-jsx", // JSXの設定（React 17以降の場合）
    "module": "ESNext",
    "target": "ESNext",
    "lib": ["DOM", "ESNext"],
    "allowSyntheticDefaultImports": true,
    "esModuleInterop": true,
    "strict": true,
    "moduleResolution": "Node",
    "skipLibCheck": true,
    "baseUrl": ".", // プロジェクトのルートを基準にする
    "paths": {
      "@/*": ["./src/*"] // "@" を "src" ディレクトリにマッピング
    }
  },
  "include": ["src/**/*"], // コンパイル対象を "src" 以下のすべてのファイルに設定
  "exclude": ["node_modules"] // "node_modules" をコンパイル対象から除外
}